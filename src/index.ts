const log = (message: string): void => console.log(message);

log(`
--------------------------------------------------------
|     Node with typescript development environment     |
--------------------------------------------------------
`);